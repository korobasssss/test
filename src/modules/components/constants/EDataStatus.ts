export enum EDeviceStatus {
  ONLINE = 'Online',
  OFFLINE = 'Offline',
}

export enum ECableStatus {
  BUCKLED = 'Buckled',
  UNBUCKLED = 'Unbuckled',
}

export enum ECableStatusTranslate {
  BUCKLED = 'Пристегнут',
  UNBUCKLED = 'Отстегнут',
}